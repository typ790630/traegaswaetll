(async()=>{const d=localStorage.getItem('app-store');if(!d)return console.log('❌ 无数据');const s=JSON.parse(d);const w=s.state?.wallets?.[0];console.log('📍 地址:',w.address);const r=await fetch('https://bsc-dataseed.binance.org/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',method:'eth_getBalance',params:[w.address,'latest'],id:1})});const res=await r.json();const bal=Number(BigInt(res.result))/1e18;console.log('💰 Smart Account BNB:',bal.toFixed(6));console.log(bal>=0.001?'✅ 足够':'❌ 不足 ('+bal.toFixed(6)+' BNB)');console.log('💡 解决方案:',bal<0.001?'需要至少 0.01 BNB 或修复 Paymaster':'检查 Paymaster');})()
